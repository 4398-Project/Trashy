<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="trashy.txstate.cs4398.sm4.trashy">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Trash"
            android:label="@string/title_activity_trash"
            android:parentActivityName=".View.Login">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="trashy.txstate.cs4398.sm4.trashy.View.Login" />
        </activity>
        <activity
            android:name=".View.Trashy_Leaderboard"
            android:label="@string/title_activity_trashy__leaderboard"></activity>


    </application>

</manifest>